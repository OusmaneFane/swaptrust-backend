# Déploiement serveur : image publiée par la CI (GHCR).
# Sur la machine : un fichier .env à la racine du dossier de déploiement (DATABASE_URL, REDIS_*, JWT_*, etc.).
# Exemple :
#   export API_IMAGE=ghcr.io/org/swap-trust-backend:staging
#   docker compose -f docker-compose.prod.yml pull api
#   docker compose -f docker-compose.prod.yml up -d api

services:
  api:
    image: ${API_IMAGE:?Set API_IMAGE to the full ghcr.io/... image reference}
    restart: unless-stopped
    ports:
      - '${API_PORT:-3001}:3001'
    env_file:
      - path: .env
        required: false
    volumes:
      - uploads_data:/app/uploads

volumes:
  uploads_data:
